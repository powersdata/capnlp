#loads needed libraries for code
#main code
if (which_library == "main") {
library(RWeka)
library(tm)
library(gdata)
        require(tm)
        require(stringr)
        #require(stringi)
#library(wordcloud)
#library(RColorBrewer)
#library(ggplot2)
}

#imports code
if (which_library == "imports") {
#        require(RWeka)
#        require(tm)
#        require(gdata)
        #library(wordcloud)
        #library(RColorBrewer)
        #library(ggplot2)
}